Search tasks by keyword or ID, displaying abbreviated task information.

Examples:
- "auth" -> Finds tasks with "auth" in name/desc
- "task-123" -> Finds specific task by ID
- "database schema" -> Fuzzy search for concepts
